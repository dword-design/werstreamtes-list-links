import { defineConfig } from 'wxt';

export default defineConfig({
  autoIcons: { developmentIndicator: false },
  manifest: { name: 'WerStreamt.es Listen Links' },
  modules: ['@wxt-dev/auto-icons'],
});
